# Go-Data-Structures
This repo contains examples of popular data structure implementations in the Go programming language.

To test your implementation of a data structure, navigate to the `data_structures/starter_code` directory and write your code where there are `TODO` comments.

Once you are done, you can test your implementation by running
`go test` within the `data_structures/starter_code` directory.
